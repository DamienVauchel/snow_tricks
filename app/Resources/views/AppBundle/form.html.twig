{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group row">
        {{ form_label(form.title, "Nom du Trick", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        {{ form_errors(form.title) }}
        <div class="col-sm-10">
            {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <br>
    <div class="form-group row">
        {{ form_label(form.description, "Description du trick", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        {{ form_errors(form.description) }}
        <div class="col-sm-10">
            <div class="text-center row info-div">
                <div class="col-xs-1">
                    <i class="fa fa-info-circle" aria-hidden="true"></i>
                </div>
                <div class="info col-xs-11">
                    <small><em>Pour ajouter une ou plusieurs vidéo(s), appuyez sur le bouton "Source", collez le(s) lien(s) embed, puis rappuyez sur "Source" pour revenir au mode visuel</em></small>
                </div>
            </div>
            {{ form_widget(form.description, {'attr': {'class': 'form-control ckeditor'}}) }}
        </div>
    </div>
    <br>
    <div class="form-group row">
        {{ form_label(form.level, "Niveau du trick", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        {{ form_errors(form.level) }}
        <div class="col-sm-10">
            {{ form_widget(form.level) }}
        </div>
    </div>
    <br>
    <div class="form-group row">
        {{ form_label(form.category, "Groupe du trick", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        {{ form_errors(form.category) }}
        <div class="col-sm-10">
            {{ form_widget(form.category) }}
        </div>
    </div>
    <div class="form-group row">
        {{ form_label(form.images, "Images", {'label_attr': {'class': 'control-label'}}) }}
        {{ form_errors(form.images) }}
        {% if trick.images is defined %}
            {% for image in trick.images %}
                <div class="text-right">
                    <a href="{{ path('delete_image', {'id': image.id}) }}" class="btn btn-danger" style="position:absolute; margin-left: -50px; margin-top: 10px"><i class="fa fa-trash-o" aria-hidden="true"></i></a>
                </div>
                <img style="width: 200px;" src="{{ asset(image.webPath) }}" alt="{{ image.alt }}">
            {% endfor %}
        {% endif %}
        {{ form_widget(form.category) }}
    </div>
    {{ form_rest(form) }}
{{ form_end(form) }}